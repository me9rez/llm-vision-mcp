# 通用 OpenAI 兼容视觉供应商配置
API_KEY=your_key_here

# 接口地址（默认 ModelScope，可替换为任意 OpenAI 兼容端点）
# BASE_URL=https://api-inference.modelscope.cn/v1

# 视觉模型（默认 Qwen/Qwen3-VL-8B-Instruct）
# VISION_MODEL=Qwen/Qwen3-VL-8B-Instruct

# 可选调参（默认 0.3 / 32768）
# TEMPERATURE=0.3
# MAX_TOKENS=32768
